## 走迷宫（方向向量旋转表）

在二维空间里“系统地”尝试扩张，方向向量就是最简化后的形式：

```CPP
int dy[4] = {1, -1, 0, 0};
int dx[4] = {0, 0, 1, -1};
```

这让 BFS/DFS 能够用统一的 code 模板在上下左右走，不用写 if-else。这在实际竞赛和开发里都非常常用。
而要求你一定按照某个顺序探索（比如永远靠右手走、靠墙走），或者机器人先朝上、再右、再下…
这时会把方向向量视为“循环队列”使用：

```CPP
int dy[4] = {0, 1, 0, -1};
int dx[4] = {1, 0, -1, 0};
//需要“右转”就 `dir = (dir + 1) % 4`。
```

提到的“旋转表”  就是**方位枚举的规律化**​。
方向向量告诉你“能怎么动”。
BFS/DFS 告诉你“按什么顺序动”。

* **DFS = 一条路走到底（堆栈式扩张）**
* **BFS = 从入口发散（层级扩张）**

只要网格是静态的（不涉及大量状态组合），这种结构几乎永远可用。

### 迷宫问题

包括：

* 最短路（BFS）
* 连通块计数（DFS）
* 找路径（BFS + parent）
* 判定是否有路（DFS）
* 迷宫生成（DFS）
* 右手法则、左手法则（方向数组 + 旋转）
* 多源点 BFS（从多个起点扩张）
* 多终点最小代价（同上）
* 波纹扩散、火焰扩散（多源 BFS）
* 回溯类（DFS + visited 回溯）


## 黑白棋

### 俩棋间夹，翻转问题
给定一个坐标，上下左右，斜对角之间遇到某条件，将其之间的棋子全部翻转。

思路明确，从给定顶点出发，沿途（使用方向向量实现沿途递增）移动，并检查是有相夹，若hasamu，则break并翻转沿途棋子，若无，则记录沿途路径为将来翻转做准备。

斜夹角翻转典例：
```cpp
int main(){
    int H,W,Y,X;
    std::cin>>H>>W>>Y>>X;

    std::vector<std::string> board(H);
    for(int i =0;i<H;i++) std::cin>>board[i];

    board[Y][X] = '*';

    
    int dpx[] = {1,1,-1,-1};
    int dpy[] = {-1,1,1,-1};

    for(int i =0;i<4;i++){
        int ty = Y;
        int tx = X;
        
        std::vector<std::pair<int,int>> path;
        bool hasamu  = false;
        while(true){
            ty += dpy[i];
            tx += dpx[i];
            if (ty < 0 || ty >= H || tx < 0 || tx >= W) break;
            if (board[ty][tx] == '*'){
                hasamu = true;break;
            }
            path.push_back({ty,tx});

        }
        if(hasamu){
            for (const auto &p : path) {
                board[p.first][p.second] = '*';
            }
        }
    }

    std::cout<<"--test--"<<std::endl;
    // 输出结果
    for (int i = 0; i < H; i++) {
        std::cout << board[i] << "\n";
    }

    return 0;
}
```









